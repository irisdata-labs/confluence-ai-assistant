[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
google-generativeai = ">=0.3.0"
python-dotenv = ">=1.0.0"
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
pytest-mock = ">=3.10.0"
coverage = ">=7.0.0"
mock = ">=4.0.3"

[dev-packages]

[requires]
python_version = "3.13"
